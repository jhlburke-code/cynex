{
  "name": "cynex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy ./dist --project-name cynex"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^11.0.0",
    "@supabase/supabase-js": "^2.45.0",
    "astro": "^4.16.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.90.0"
  }
}
